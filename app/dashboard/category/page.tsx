'use client';
import { Box } from '@chakra-ui/react';

export default function Category() {
    return (
        <Box sx={{ minHeight: '100vh' }} p={'5'}>
            category
        </Box>
    );
}
