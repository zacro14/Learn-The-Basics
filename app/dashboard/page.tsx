'use client';
import { Box, Text } from '@chakra-ui/react';

export default function Dashboard() {
    return (
        <Box sx={{ minHeight: '100vh' }} p={'5'}>
            DashBoard
        </Box>
    );
}
